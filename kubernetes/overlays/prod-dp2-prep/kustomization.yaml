images:
- name: ghcr.io/lsst-dm/ctrl_ingestd
  newTag: "2.1"

namespace: prod-dp2-prep

replicas:
- name: ingestd
  count: 1

resources:
- ../../common
- ns.yaml

configMapGenerator:
- name: ingestd-config
  files:
  - ingestd.yml
secretGenerator:
- name: postgres
  files:
  - postgres-user=etc/.secrets/user
  - postgres-password=etc/.secrets/password
